# Image Loader - Загрузчик изображений с постов форума

[![CI](https://github.com/sni10/Universal-forum-parcer/actions/workflows/ci.yml/badge.svg)](https://github.com/sni10/Universal-forum-parcer/actions/workflows/ci.yml)
[![Release](https://github.com/sni10/Universal-forum-parcer/actions/workflows/release.yml/badge.svg)](https://github.com/sni10/Universal-forum-parcer/actions/workflows/release.yml)
[![Python](https://img.shields.io/badge/Python-3.10%2B-blue?logo=python&logoColor=white)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green?logo=opensourceinitiative&logoColor=white)](https://github.com/sni10/Universal-forum-parcer/blob/main/LICENSE)
[![Latest Release](https://img.shields.io/github/v/release/sni10/Universal-forum-parcer?logo=github)](https://github.com/sni10/Universal-forum-parcer/releases/latest)
[![Tests](https://img.shields.io/badge/tests-17%20passed-brightgreen?logo=pytest&logoColor=white)](https://github.com/sni10/Universal-forum-parcer/actions/workflows/ci.yml)
[![Coverage](https://img.shields.io/badge/coverage-100%25-brightgreen?logo=codecov&logoColor=white)](https://github.com/sni10/Universal-forum-parcer/actions/workflows/ci.yml)

Асинхронный загрузчик изображений с постов форума, в том числе из внешних источников, с поддержкой восстановления после сбоев, повторными попытками и умной фильтрацией.

## Особенности

- ✅ **Автоматическая загрузка страниц**: Chrome WebDriver с антибот-защитой (undetected-chromedriver)
- ✅ **Поддержка пагинации**: автоматический переход по всем страницам форума
- ✅ **Использование вашего Chrome**: поддержка профилей Chrome (блокировщики рекламы, сессии)
- ✅ **Отслеживание страниц**: база данных страниц для восстановления после прерывания
- ✅ **Асинхронная загрузка**: 10 параллельных загрузок с ограничением на хост (3 на хост)
- ✅ **Умная фильтрация**: автоматическое исключение превью/thumbnails (Pixhost и другие)
- ✅ **Восстановление после сбоев**: автоматическое восстановление прерванных загрузок
- ✅ **Докачка файлов**: поддержка Range-запросов для продолжения прерванных загрузок
- ✅ **Retry с экспоненциальным backoff**: автоматические повторы с джиттером при ошибках
- ✅ **Антифлуд**: соблюдение Retry-After заголовков, ограничение запросов на хост
- ✅ **SQLite хранилище**: надежное хранение статусов + JSON экспорт для человека
- ✅ **Инкрементальная нумерация**: блоки нумеруются 0001_, 0002_ и т.д.
- ✅ **Безопасное именование**: санитизация имен для Windows (удаляет недопустимые символы, зарезервированные имена)
- ✅ **Атомарная запись**: использование .part файлов и os.replace для безопасности
- ✅ **Логирование**: ротация логов (10 файлов × 10 МБ) в файл и консоль
- ✅ **ООП/SOLID**: чистая архитектура с разделением ответственности

## Требования

- Python 3.10+
- Windows (оптимизировано для Windows, но работает на Linux/Mac)

## Установка

### 1. Клонировать репозиторий / скачать файлы

```bash
cd F:\HOME\load
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить конфигурацию

Скопируйте `.env.example` в `.env` и настройте параметры:

```bash
copy .env.example .env
```

Отредактируйте `.env` в текстовом редакторе:

```env
# Основные настройки
DB_PATH=data/loader.db
IMG_DIR=img
LINKS_DIR=links
LOGS_DIR=logs

# Загрузка
GLOBAL_LIMIT=10          # Макс. одновременных загрузок
PER_HOST_LIMIT=3         # Макс. загрузок на хост
MAX_RETRIES=5            # Попыток повтора
DOWNLOAD_TIMEOUT=300     # Таймаут в секундах

# Логирование
LOG_LEVEL=INFO

# Настройки форума (ОБЯЗАТЕЛЬНО для URL-режима)
FORUM_DOMAIN=https://your-forum.com
START_URL=/viewtopic.php?f=9&t=10160660
PAGE_PARAM=start         # "start", "page" или "p"

# Настройки Chrome (опционально)
CHROME_PROFILE_PATH=     # Путь к профилю Chrome (для использования блокировщиков рекламы)
CHROME_HEADLESS=false    # true = без окна, false = с окном
CHROME_BINARY_PATH=      # Путь к chrome.exe (автоопределение, если пусто)
```

### ⚠️ ВАЖНО: Организация директорий по доменам

**Программа автоматически организует файлы по доменам!**

Когда вы указываете `FORUM_DOMAIN` в `.env`, программа создает поддиректории с именем домена:

**Пример:**
```env
FORUM_DOMAIN=https://forum.example.com
DB_PATH=data/loader.db
IMG_DIR=img
LINKS_DIR=links
```

**Фактическая структура, которая будет создана:**
```
load/
├── data/
│   └── forum.example.com/       # ← Поддиректория с именем домена
│       └── loader.db             # База данных для этого форума
├── img/
│   └── forum.example.com/       # ← Поддиректория с именем домена
│       ├── 0001_Set_Name/
│       ├── 0002_Another_Set/
│       └── ...
├── links/
│   └── forum.example.com/       # ← Поддиректория с именем домена
│       ├── 0001_Set_Name/
│       ├── 0002_Another_Set/
│       └── ...
└── logs/
    └── app.log                   # Логи общие (не разделены по доменам)
```

**Почему это удобно:**
- ✅ Можно работать с несколькими форумами одновременно
- ✅ Каждый форум имеет свою базу данных со статусами
- ✅ Изображения и ссылки организованы по форумам
- ✅ Нет конфликтов при работе с разными сайтами

**Если FORUM_DOMAIN не указан:** используются базовые пути без поддиректорий.

## Использование

### 🚀 Режим 1: Автоматическая загрузка с форума (РЕКОМЕНДУЕТСЯ)

Настройте `.env` с параметрами форума и просто запустите:

```bash
python main.py
```

Программа:
1. Откроет Chrome (можно с вашим профилем и блокировщиками)
2. Загрузит стартовую страницу форума
3. Автоматически пройдет по всем страницам (пагинация)
4. Извлечет все блоки с изображениями
5. Загрузит все изображения
6. Сохранит прогресс в базе данных

**Восстановление после прерывания:**
Просто запустите снова — программа продолжит с последней обработанной страницы.

**Пример конфигурации:**

```env
FORUM_DOMAIN=https://forum.com
START_URL=/viewtopic.php?f=9&t=10160660
PAGE_PARAM=start
CHROME_HEADLESS=false    # Видеть процесс
```

### 📄 Режим 2: Обработка сохраненного HTML файла

Если у вас уже есть HTML файл:

```bash
python main.py page.html
```

### 📝 Режим 3: Обработка HTML из строки

```bash
python main.py --html "<div class='row list-row'>...</div>"
```

### Использование Chrome профиля

Чтобы использовать ваш профиль Chrome с блокировщиками рекламы:

1. Найдите путь к профилю Chrome:
   - Windows: `C:\Users\<ВашеИмя>\AppData\Local\Google\Chrome\User Data`

2. **ВАЖНО**: Закройте Chrome перед запуском

3. Укажите в `.env`:
   ```env
   CHROME_PROFILE_PATH=C:\Users\YourName\AppData\Local\Google\Chrome\User Data
   ```

**Совет**: Создайте копию профиля для скрипта, чтобы избежать конфликтов:
```bash
xcopy "C:\Users\YourName\AppData\Local\Google\Chrome\User Data" "C:\ChromeProfile" /E /I
```

Затем используйте копию:
```env
CHROME_PROFILE_PATH=C:\ChromeProfile
```

## Структура проекта

```
load/
├── src/                      # Исходный код (ядро в папке app)
│   ├── app/                  # Оркестратор (координация компонентов)
│   │   ├── __init__.py
│   │   └── orchestrator.py
│   ├── domain/               # Модели данных
│   │   ├── __init__.py
│   │   └── models.py         # PostBlock, ImageLink, LinkStatus
│   ├── storage/              # Слой хранения (SQLite)
│   │   ├── __init__.py
│   │   ├── database.py       # Схема БД
│   │   └── repository.py     # LinkRepository
│   ├── parser/               # Парсер HTML
│   │   ├── __init__.py
│   │   └── extractor.py      # LinkExtractor
│   ├── downloader/           # Асинхронный загрузчик
│   │   ├── __init__.py
│   │   ├── limiter.py        # HostLimiter
│   │   └── downloader.py     # ImageDownloader
│   ├── fs/                   # Файловые утилиты
│   │   ├── __init__.py
│   │   └── utils.py          # slugify, atomic_write_jsonl
│   ├── log/                  # Логирование
│   │   ├── __init__.py
│   │   └── logger.py
│   ├── browser/              # Браузер (Selenium)
│   │   ├── __init__.py
│   │   ├── fetcher.py        # ChromeFetcher
│   │   └── pagination.py     # PaginationHandler
│   ├── config.py             # Конфигурация из .env
│   └── __init__.py
├── data/                     # SQLite базы данных
│   └── <domain>/             # Поддиректория по домену
│       └── loader.db
├── img/                      # Загруженные изображения
│   └── <domain>/             # Поддиректория по домену
│       ├── 0001_Sample_Set_Alpha/
│       ├── 0002_Another_Set/
│       └── ...
├── links/                    # Экспорт ссылок (JSONL)
│   └── <domain>/             # Поддиректория по домену
│       ├── 0001_Sample_Set_Alpha/
│       │   ├── all_links.jsonl
│       │   ├── done_links.jsonl
│       │   └── manifest.json
│       └── ...
├── logs/                     # Логи
│   └── app.log
├── main.py                   # Точка входа
├── requirements.txt          # Зависимости
├── .env.example              # Шаблон конфигурации
├── .env                      # Ваша конфигурация (создайте вручную)
├── docs/
│   ├── info.md               # Пример HTML структуры
│   └── add.md                # Дополнительные требования
└── README.md                 # Этот файл
```

## Как это работает

### 1. Парсинг HTML

`LinkExtractor` извлекает блоки постов из HTML:
- Находит `div.row.list-row` блоки
- Извлекает заголовок из `postbody` (текст до первого `<br>`)
- Фильтрует ссылки:
  - ✅ Только `http(s)://`
  - ✅ Только изображения (`.jpg`, `.jpeg`, `.png`, `.webp`, `.gif`)
  - ❌ Исключает превью (Pixhost: `tNN.pixhost.to/thumbs/`)
  - ❌ Исключает относительные ссылки
- Дедупликация (нормализация URL)

### 2. Хранение в SQLite

`LinkRepository` управляет БД:
- Таблица `blocks`: блоки постов (id, title, slug, created_at)
- Таблица `links`: ссылки на изображения (id, block_id, url, status, filename, size, etag, retries, error, ...)
- Статусы: `new` → `queued` → `downloading` → `done` / `failed`
- Восстановление: при запуске `downloading` → `queued`

### 3. Асинхронная загрузка

`ImageDownloader`:
- 10 параллельных задач (глобальный семафор)
- 3 задачи на хост (per-host семафор)
- Retry с экспоненциальным backoff и джиттером
- Соблюдение `Retry-After` (429, 503)
- Атомарная запись: `.part` → `os.replace()`
- Докачка: `Range: bytes=N-`
- Валидация: `Content-Type: image/*`

### 4. Экспорт JSONL

После обработки блока:
- `links/<domain>/<slug>/all_links.jsonl` - все ссылки
- `links/<domain>/<slug>/done_links.jsonl` - успешно загруженные
- `links/<domain>/<slug>/manifest.json` - метаданные (счетчики, дата)

### 5. Логирование

- Файл: `logs/app.log` (ротация: 10 × 10 МБ)
- Консоль: дублирование важных сообщений
- Формат: `YYYY-MM-DD HH:MM:SS | LEVEL | logger | message`

## Восстановление после сбоев

При прерывании процесса:

1. **Автоматически при следующем запуске**:
   - Ссылки в статусе `downloading` → `queued`
   - Продолжение загрузки с места остановки

2. **Докачка файлов**:
   - Если `.part` файл существует → продолжить с `Range`
   - Проверка размера и `ETag`

3. **Идемпотентность**:
   - Повторный запуск безопасен
   - Блоки с существующим slug не пересоздаются
   - Уже загруженные файлы не перезагружаются

## Примеры вывода

```
=== Configuration ===
DB_PATH: data/forum.example.com/loader.db
IMG_DIR: img/forum.example.com
LINKS_DIR: links/forum.example.com
LOGS_DIR: logs
GLOBAL_LIMIT: 10
PER_HOST_LIMIT: 3
MAX_RETRIES: 5
DOWNLOAD_TIMEOUT: 300s
==============================

Processing block 1: Sample Collection Alpha - Demo (150 links)
Downloading 150 images for block 0001_Sample Collection Alpha - Demo
Block 0001_Sample Collection Alpha - Demo complete: 148 done, 2 failed, 0 pending

============================================================
Processing complete!
============================================================
Block: 0001_Sample Collection Alpha - Demo
  Title: Sample Collection Alpha - Demo
  Total links: 150
  Downloaded: 148
  Failed: 2
  Queued: 0
============================================================
```

## Безопасность

- ✅ Санитизация имен файлов/папок (Windows)
- ✅ Проверка путей (внутри корня проекта)
- ✅ Блокировка `file://`, `javascript:`, `data:` ссылок
- ✅ Валидация `Content-Type`
- ✅ TLS сертификаты включены
- ✅ Атомарная запись (нет поврежденных файлов)

## Устранение неполадок

### ModuleNotFoundError

```bash
pip install -r requirements.txt
```

### Ошибки кодирования

Убедитесь, что HTML файлы в UTF-8.

### Недостаточно прав на запись

Запустите от администратора или проверьте права на папки `img/`, `links/`, `data/`, `logs/`.

### Все загрузки failed

Проверьте:
- Интернет соединение
- Firewall/антивирус не блокирует
- URL действительно ведут на изображения

## Лицензия

Этот проект создан для личного использования.

## Автор

Разработано по техническому заданию с учетом всех требований безопасности, надежности и восстановления после сбоев.
